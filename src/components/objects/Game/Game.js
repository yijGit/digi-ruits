
class Game {
    constructor(parent){
        this.parent = parent;
        this.state{
            gui: parent.state.gui,
            yellow_remain = 12,
            blue_remain = 12,
            game_over = false,
            ball_instances = 0,
        }
        parent.addToUpdateList(this);
    }
    rerack(rack){
        if
    }

    update(){
        if(this.parent.gui.)
    }
}

export default Game;